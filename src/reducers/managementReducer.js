// import {CREATE_PRODUCT_SUCCESS} from '../actions/actionCreateProduct';

export function managementReducer(state = {}, action) {
    switch (action.type) {
        default:
            return state;
    }
}
